import React from "react";
import { DayPickerRangeProps, DayPickerSingleProps } from "react-day-picker";
declare function Calendar<T extends DayPickerSingleProps | DayPickerRangeProps>({ mode, defaultMonth, selected, onSelect, locale, disabled, classNames, ...other }: T): React.JSX.Element;
declare namespace Calendar {
    var displayName: string;
}
export default Calendar;
